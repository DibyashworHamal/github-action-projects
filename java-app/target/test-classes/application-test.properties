# ── DATABASE: replace MySQL with H2 in-memory ──────────────────
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL;NON_KEYWORDS=VALUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# ── JPA / Hibernate ────────────────────────────────────────────
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# ── MAIL: disable real Gmail for tests ─────────────────────────
spring.mail.host=localhost
spring.mail.port=3025
spring.mail.username=test
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# ── KEEP these the same as main (safe to repeat) ───────────────
jwt.secret=WW91clN1cGVyU2VjcmV0S2V5Rm9ySldUc1RoYXRJc0xvbmdBbmRTZWN1cmU=
jwt.expiration=86400000
file.upload-dir=src/main/resources/static/uploads/organizer_docs

# S3 Dummy Configuration for Unit Tests
AWS_S3_ENDPOINT=http://localhost:9000
AWS_S3_ACCESS_KEY=dummy_access_key
AWS_S3_SECRET_KEY=dummy_secret_key
AWS_S3_REGION=us-east-1
AWS_S3_BUCKET_NAME=dip-uploads
AWS_S3_PUBLIC_URL=http://localhost:9000